------------------------HOME------------------------------------
----prende le 3 news piu recenti (se presenti nel db)-----------

SELECT * 
FROM `NEWS`
ORDER BY data DESC, ora DESC
LIMIT 3

----------------------------------------------------------------
----prende le news di oggi-----------

SELECT COUNT(data)
FROM `NEWS` 
WHERE data=CURDATE()